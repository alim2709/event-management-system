type User @node {
  id: ID! @id                     
  username: String! @unique
  hashedPassword: String!          
  email: String!  @unique
  events: [Event!]! @relationship(type: "CREATED", direction: OUT)           
}
